# render.yaml
services:
  - type: web
    name: fx1-hubs-nebula
    env: static
    plan: free
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    autoDeploy: true

    routes:
      - type: rewrite
        source: /**
        destination: /index.html

    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=0, must-revalidate

    envVars:
      - key: NODE_ENV
        value: production
      - key: FARCASTER_MINI_APP
        value: true
      - key: BASE_CHAIN_ID
        value: 8453
      - key: APP_NAME
        value: fx1-nebula-gallery